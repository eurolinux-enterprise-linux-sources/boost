--- boost/exception_ptr.hpp	(revision 57764)
+++ boost/exception_ptr.hpp	(revision 57765)
@@ -90,6 +90,10 @@ 
             {
             }
 
+        ~exception_ptr() throw()
+            {
+            }
+
         operator unspecified_bool_type() const
             {
             return _empty() ? 0 : &exception_ptr::bad_alloc_;
